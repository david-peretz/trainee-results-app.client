export interface ChartData {
    id: number;      // Unique identifier for the data point
    label: string;  // Label for the data point (e.g., a category or subject name)
    value: number;  // Numeric value associated with the label (e.g., a score or percentage)
  }
  